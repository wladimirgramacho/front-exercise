<section id="section-registration" class="d-flex justify-content-center align-items-center">
  <div class="sign-up">
    <h3>Cadastro</h3>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email, class: "sign-label" %><br />
        <%= f.email_field :email, autofocus: true, class: "sign-input" %>
      </div>

      <div class="field">
        <%= f.label :password, class: "sign-label" %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off", class: "sign-input" %>
      </div>

      <div class="field">
        <%= f.label :password_confirmation, class: "sign-label" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: "sign-input" %>
      </div>

      <div class="actions d-flex justify-content-end mt-2">
        <%= f.submit "Sign up", class: "btn btn-BB" %>
      </div>
    <% end %>

  </div>
</section>