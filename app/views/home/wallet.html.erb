<section>
	<h4>
	  Bem-vindo(a)
	  <%= current_user.name %>
	</h4>

	<br>


	<% @wallet.each do |coin_id, total| %>
		<%= Coin.find(coin_id).name %>
		<%= total %>
		<%= link_to "Vender", transactions_sell_path(coin_id) %>
		<br>
	<% end %>

	<%= link_to 'Comprar moedas', transactions_new_path %>
</section>